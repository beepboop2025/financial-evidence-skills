# Marketplace assets

- `logo.svg` is the canonical 512 by 512 vector mark.
- `logo-400.png` is the lossless 400 by 400 marketplace icon generated from the
  canonical vector mark for directories that require PNG uploads.

The PNG must remain visually equivalent to `logo.svg`; it is a compatibility
asset, not a separate brand variant.
